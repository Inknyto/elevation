/* JobMap.css */
.job-map-overlay {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
    filter: invert(100%) hue-rotate(180deg) brightness(95%) contrast(90%);
}

.job-map-container {
  position: relative;
  width: 60%; /* Adjust as needed */
  max-width: 800px; /* Maximum width for the map container */
  height: 100%; /* Adjust as needed */
  max-height: 1000px; /* Maximum height for the map container */
  border-radius: 8px;
  overflow: hidden;
}

.close-button {
  position: absolute;
  top: 10px;
  right: 10px;
  padding: 8px 12px;
  background: #fff;
  border: 1px solid #ccc;
  border-radius: 4px;
  cursor: pointer;
}
